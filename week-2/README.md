# Week 2 — RAG / vector databases

Class lab notebook (embeddings, chunking, retrieval). Graded Session 2 Path A (ingest + RAG `/ask`) lives in **`../week-1/`**.

```bash
cd rag-vector-databases
# follow that folder's README / notebook
```
